naftiko: 1.0.0-alpha2
info:
  label: International Business Machines IBM Cloud VPC API — Subnets
  description: 'International Business Machines IBM Cloud VPC API — Subnets. 1 operations. Lead operation: International Business
    Machines List subnets. Self-contained Naftiko capability covering one International Business Machines business surface.'
  tags:
  - International Business Machines
  - Subnets
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    INTERNATIONAL_BUSINESS_MACHINES_API_KEY: INTERNATIONAL_BUSINESS_MACHINES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ibm-vpc-subnets
    baseUri: https://us-south.iaas.cloud.ibm.com
    description: International Business Machines IBM Cloud VPC API — Subnets business capability. Self-contained, no shared
      references.
    resources:
    - name: v1-subnets
      path: /v1/subnets
      operations:
      - name: listsubnets
        method: GET
        description: International Business Machines List subnets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: version
          in: query
          type: string
          required: true
        - name: generation
          in: query
          type: integer
          required: true
    authentication:
      type: bearer
      token: '{{env.INTERNATIONAL_BUSINESS_MACHINES_API_KEY}}'
  exposes:
  - type: rest
    namespace: ibm-vpc-subnets-rest
    port: 8080
    description: REST adapter for International Business Machines IBM Cloud VPC API — Subnets. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v1/subnets
      name: v1-subnets
      description: REST surface for v1-subnets.
      operations:
      - method: GET
        name: listsubnets
        description: International Business Machines List subnets
        call: ibm-vpc-subnets.listsubnets
        with:
          version: rest.version
          generation: rest.generation
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ibm-vpc-subnets-mcp
    port: 9090
    transport: http
    description: MCP adapter for International Business Machines IBM Cloud VPC API — Subnets. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: international-business-machines-list-subnets
      description: International Business Machines List subnets
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ibm-vpc-subnets.listsubnets
      with:
        version: tools.version
        generation: tools.generation
      outputParameters:
      - type: object
        mapping: $.
