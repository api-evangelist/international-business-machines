naftiko: 1.0.0-alpha2
info:
  label: International Business Machines IBM Cloud VPC API — Security Groups
  description: 'International Business Machines IBM Cloud VPC API — Security Groups. 1 operations. Lead operation: International
    Business Machines List security groups. Self-contained Naftiko capability covering one International Business Machines
    business surface.'
  tags:
  - International Business Machines
  - Security Groups
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    INTERNATIONAL_BUSINESS_MACHINES_API_KEY: INTERNATIONAL_BUSINESS_MACHINES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ibm-vpc-security-groups
    baseUri: https://us-south.iaas.cloud.ibm.com
    description: International Business Machines IBM Cloud VPC API — Security Groups business capability. Self-contained,
      no shared references.
    resources:
    - name: v1-security_groups
      path: /v1/security_groups
      operations:
      - name: listsecuritygroups
        method: GET
        description: International Business Machines List security groups
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: version
          in: query
          type: string
          required: true
        - name: generation
          in: query
          type: integer
          required: true
    authentication:
      type: bearer
      token: '{{env.INTERNATIONAL_BUSINESS_MACHINES_API_KEY}}'
  exposes:
  - type: rest
    namespace: ibm-vpc-security-groups-rest
    port: 8080
    description: REST adapter for International Business Machines IBM Cloud VPC API — Security Groups. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v1/security-groups
      name: v1-security-groups
      description: REST surface for v1-security_groups.
      operations:
      - method: GET
        name: listsecuritygroups
        description: International Business Machines List security groups
        call: ibm-vpc-security-groups.listsecuritygroups
        with:
          version: rest.version
          generation: rest.generation
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ibm-vpc-security-groups-mcp
    port: 9090
    transport: http
    description: MCP adapter for International Business Machines IBM Cloud VPC API — Security Groups. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: international-business-machines-list-security
      description: International Business Machines List security groups
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ibm-vpc-security-groups.listsecuritygroups
      with:
        version: tools.version
        generation: tools.generation
      outputParameters:
      - type: object
        mapping: $.
